#Quiz Maker Features

#Start message
#Options
    #Option one (create new quiz)
    #Option two (edit existing)
    #Option three (exit)
    #Features in both options one and two
        #input questions and answer
        #options list
        #questions with choice and answer
        # file write
        # replay
    #features in all three
        #Confirmation message
#Replay

# Classes to create(?)
    #option one
    #option two (inherit text_messages to change message)